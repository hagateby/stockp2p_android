package com.ktsf.framework.longin;import android.os.Bundle;import com.stockp2p.R;import com.ktsf.common.util.TipUitls;import com.ktsf.framework.BaseFragmentActivity;import com.ktsf.framework.Framework;/** * 密码找回 *  * @author haix *  */public class ForgetPassWordActivity extends BaseFragmentActivity {	private static String TAG = "ForgetPassWordActivity";	private String loginPass;	@Override	public void onCreate(Bundle savedInstanceState) {		// TODO Auto-generated method stub		super.onCreate(savedInstanceState);		if (getIntent() != null) {			if (getIntent().getStringExtra("loginPass") != null) {				loginPass = getIntent().getStringExtra("loginPass");			}		}  /*		thisManager				.beginTransaction()				.replace(R.id.tab_container,						new ForgetPasswordWaySelect(loginPass))				.addToBackStack(TAG).commit(); */	}}