package com.pactera.nci.common.view;import android.content.Context;import android.util.AttributeSet;import android.view.View;import android.widget.LinearLayout;import com.pactera.nci.R;import com.pactera.nci.common.view.LPScrollLayout.OnScreenChangeListener;public class LPPageControlView extends LinearLayout {	private Context context;	private int count;	public LPPageControlView(Context context) {		super(context);		this.context = context;	}	public LPPageControlView(Context context, AttributeSet attrs) {		super(context, attrs);		this.context = context;	}	public void bindScrollLayout(LPScrollLayout scrollLayout, int flag) {		this.count = scrollLayout.getChildCount();		//final int flag_temp = flag;		// 初始化第�?��		generatePageControl(0);		scrollLayout.setOnScreenChangeListener(new OnScreenChangeListener() {			@Override			public void onScreenChange(int currentIndex) {				generatePageControl(currentIndex);			}		});	}	public void generatePageControl(int currentIndex) {		this.removeAllViews();		for (int i = 0; i < this.count; i++) {			LinearLayout dotLayout = (LinearLayout)View.inflate(context, R.layout.quicksrv_ad_images_dot_item, null);			View dot = dotLayout.findViewById(R.id.ad_dot);			if (i == currentIndex) {				dot.setBackgroundResource(R.drawable.framework_dot_foc);			} else {				dot.setBackgroundResource(R.drawable.quicksrv_dot_nor);			}			this.addView(dotLayout);		}	}}